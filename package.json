{
  "name": "serverless-ts-project",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "deploy": "serverless deploy",
    "build": "esbuild handler.ts --bundle --platform=node --target=node18 --outfile=.build/handler.js"
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0",
    "esbuild": "^0.25.5",
    "serverless": "^4.15.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.823.0",
    "@aws-sdk/lib-dynamodb": "^3.823.0",
    "@types/aws-lambda": "^8.10.120",
    "node-fetch": "^3.3.2",
    "uuid": "^11.1.0"
  }
}
